import {EnumDialogs} from "../components/modal-dialogs/enum-dialogs";

export class ModelDialog {
    public createRoom = EnumDialogs.CreateRoom;
    public authentication = EnumDialogs.Authentication;
    public remindPassword = EnumDialogs.RemindPassword;

    constructor () {
    }

}